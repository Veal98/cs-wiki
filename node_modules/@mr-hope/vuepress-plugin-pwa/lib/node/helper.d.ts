import type { ManifestOption, PWAOptions } from "../types";
export declare const appendBasetoManifest: (base: string, manifest: ManifestOption) => ManifestOption;
export declare const appendBase: (base: string, options: PWAOptions) => void;
