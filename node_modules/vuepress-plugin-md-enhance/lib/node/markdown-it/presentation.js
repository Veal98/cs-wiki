"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable max-statements */
const hash = require("hash-sum");
const utils_1 = require("./utils");
exports.default = (md) => {
    md.block.ruler.before("fence", "presentation", (0, utils_1.generateUML)({
        name: "presentation",
        open: "slidestart",
        close: "slideend",
    }), { alt: ["paragraph", "reference", "blockquote", "list"] });
    md.renderer.rules.presentation = (tokens, idx) => {
        const token = tokens[idx];
        const key = `presentation-${hash(idx)}`;
        const { content, info } = token;
        return `<Presentation id="${key}" data-code="${encodeURIComponent(content)}" theme="${info.trim() || "auto"}"></Presentation>`;
    };
};
//# sourceMappingURL=presentation.js.map