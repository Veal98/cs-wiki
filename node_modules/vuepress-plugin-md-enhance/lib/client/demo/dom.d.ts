import type { CodeType, Code } from "./typings";
export declare const select: (node: Element | Document, selector: string) => HTMLElement[];
export interface DOMInitOption {
    container: HTMLElement;
    code: Code;
    codeType: CodeType;
    innerHTML?: boolean;
}
export declare const initDom: ({ code, codeType, container, innerHTML, }: DOMInitOption) => void;
