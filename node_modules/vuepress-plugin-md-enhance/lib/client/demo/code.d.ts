import type Babel from "@babel/core";
import type { Code, CodeType } from "./typings";
import type { CodeDemoOptions } from "../../types";
declare global {
    interface Window {
        Babel: typeof Babel;
    }
}
export declare const getCode: (code: Record<string, string>) => CodeType;
export declare const wrapper: (scriptStr: string) => string;
export declare const getNormalCode: (code: CodeType, config: Partial<CodeDemoOptions>) => Code;
export declare const getVueCode: (code: CodeType, config: Partial<CodeDemoOptions>) => Code;
export declare const getReactCode: (code: CodeType, config: Partial<CodeDemoOptions>) => Code;
