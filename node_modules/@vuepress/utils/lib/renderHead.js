"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderHead = void 0;
const renderHeadAttrs_1 = require("./renderHeadAttrs");
/**
 * Render head config to string
 */
const renderHead = ([tag, attrs, innerHTML = '',]) => {
    const openTag = `<${tag}${(0, renderHeadAttrs_1.renderHeadAttrs)(attrs)}>`;
    if (tag === 'link' || tag === 'meta' || tag === 'base') {
        return openTag;
    }
    return `${openTag}${innerHTML}</${tag}>`;
};
exports.renderHead = renderHead;
