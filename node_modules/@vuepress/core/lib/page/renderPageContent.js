"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderPageContent = void 0;
/**
 * Render page content and extract related info
 */
const renderPageContent = async ({ app, content, frontmatter, filePath, filePathRelative, }) => {
    const markdownEnv = {
        base: app.options.base,
        filePath,
        filePathRelative,
        frontmatter,
    };
    const contentRendered = app.markdown.render(content, markdownEnv);
    /* istanbul ignore next */
    const { headers = [], hoistedTags = [], importedFiles = [], links = [], title = '', } = markdownEnv;
    return {
        contentRendered,
        deps: importedFiles,
        headers,
        hoistedTags,
        links,
        title,
    };
};
exports.renderPageContent = renderPageContent;
