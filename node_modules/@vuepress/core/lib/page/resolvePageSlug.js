"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePageSlug = void 0;
const utils_1 = require("@vuepress/utils");
const DATE_RE = /(\d{4}-\d{1,2}(-\d{1,2})?)-(.*)/;
/**
 * Resolve page slug from filename
 */
const resolvePageSlug = ({ filePathRelative, }) => {
    if (!filePathRelative) {
        return '';
    }
    const filename = utils_1.path.parse(filePathRelative).name;
    const match = filename.match(DATE_RE);
    return match ? match[3] : filename;
};
exports.resolvePageSlug = resolvePageSlug;
