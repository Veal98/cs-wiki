"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePageFilePath = void 0;
const utils_1 = require("@vuepress/utils");
/**
 * Resolve absolute and relative path of page file
 */
const resolvePageFilePath = ({ app, options, }) => {
    // empty file path
    if (!options.filePath) {
        return {
            filePath: null,
            filePathRelative: null,
        };
    }
    if (!utils_1.path.isAbsolute(options.filePath)) {
        throw utils_1.logger.createError(`filePath is not absolute file path: ${options.filePath}}`);
    }
    return {
        filePath: options.filePath,
        filePathRelative: utils_1.path.relative(app.dir.source(), options.filePath),
    };
};
exports.resolvePageFilePath = resolvePageFilePath;
