"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePagePath = void 0;
const shared_1 = require("@vuepress/shared");
const utils_1 = require("@vuepress/utils");
/**
 * Resolve the final route path of a page
 */
const resolvePagePath = ({ permalink, pathInferred, options, }) => {
    let pagePath = options.path || permalink || pathInferred;
    if (!pagePath) {
        throw utils_1.logger.createError(`page path is empty, page options: ${JSON.stringify(options, null, 2)}`);
    }
    if (!pagePath.endsWith('.html')) {
        pagePath = (0, shared_1.ensureEndingSlash)(pagePath);
    }
    return encodeURI(pagePath);
};
exports.resolvePagePath = resolvePagePath;
