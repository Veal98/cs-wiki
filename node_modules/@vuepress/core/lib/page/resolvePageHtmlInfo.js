"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePageHtmlInfo = void 0;
const shared_1 = require("@vuepress/shared");
/**
 * Resolve page rendered html file path
 */
const resolvePageHtmlInfo = ({ app, path: pagePath, }) => {
    // /foo.html -> foo.html
    // /foo/ -> foo/index.html
    const htmlFilePathRelative = (0, shared_1.removeLeadingSlash)(decodeURI(pagePath.replace(/\/$/, '/index.html')));
    const htmlFilePath = app.dir.dest(htmlFilePathRelative);
    return {
        htmlFilePath,
        htmlFilePathRelative,
    };
};
exports.resolvePageHtmlInfo = resolvePageHtmlInfo;
