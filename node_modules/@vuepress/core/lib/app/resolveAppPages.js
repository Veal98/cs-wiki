"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveAppPages = void 0;
const utils_1 = require("@vuepress/utils");
const page_1 = require("../page");
const log = (0, utils_1.debug)('vuepress:core/app');
/**
 * Resolve pages for vuepress app
 */
const resolveAppPages = async (app) => {
    log('resolveAppPages start');
    // resolve page absolute file paths according to the page patterns
    const pageFilePaths = await (0, utils_1.globby)(app.options.pagePatterns, {
        absolute: true,
        cwd: app.dir.source(),
    });
    // create pages from files
    const pages = await Promise.all(pageFilePaths.map((filePath) => (0, page_1.createPage)(app, { filePath })));
    // if there is no 404 page, add one
    if (!pages.some((page) => page.path === '/404.html')) {
        pages.push(await (0, page_1.createPage)(app, {
            path: '/404.html',
            frontmatter: {
                layout: '404',
            },
        }));
    }
    log('resolveAppPages finish');
    return pages;
};
exports.resolveAppPages = resolveAppPages;
