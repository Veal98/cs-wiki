"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareLayoutComponents = void 0;
/**
 * Generate layout components temp file
 */
const prepareLayoutComponents = async (app) => {
    const content = `\
import { defineAsyncComponent } from 'vue'

export const layoutComponents = {\
${Object.entries(app.layouts)
        .map(([name, path]) => `
  ${JSON.stringify(name)}: defineAsyncComponent(() => import(${app.env.isDebug ? `/* webpackChunkName: "layout-${name}" */` : ''}${JSON.stringify(path)})),`)
        .join('')}
}
`;
    await app.writeTemp('internal/layoutComponents.js', content);
};
exports.prepareLayoutComponents = prepareLayoutComponents;
