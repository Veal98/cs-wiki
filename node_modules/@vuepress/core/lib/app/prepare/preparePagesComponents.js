"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.preparePagesComponents = void 0;
/**
 * Generate page key to page component map temp file
 */
const preparePagesComponents = async (app) => {
    // generate page component map file
    const content = `\
import { defineAsyncComponent } from 'vue'

export const pagesComponents = {\
${app.pages
        .map(({ key, path, componentFilePath, componentFileChunkName }) => `
  // path: ${path}
  ${JSON.stringify(key)}: defineAsyncComponent(() => import(${componentFileChunkName
        ? `/* webpackChunkName: "${componentFileChunkName}" */`
        : ''}${JSON.stringify(componentFilePath)})),`)
        .join('')}
}
`;
    await app.writeTemp('internal/pagesComponents.js', content);
};
exports.preparePagesComponents = preparePagesComponents;
