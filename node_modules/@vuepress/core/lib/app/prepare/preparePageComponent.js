"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.preparePageComponent = void 0;
/**
 * Generate page component temp file of a single page
 */
const preparePageComponent = async (app, page) => {
    await app.writeTemp(page.componentFilePathRelative, [
        // take the rendered markdown content as <template>
        `<template>${page.contentRendered}</template>\n`,
        // hoist `<script>`, `<style>` and other custom blocks
        ...page.hoistedTags,
    ].join('\n'));
};
exports.preparePageComponent = preparePageComponent;
