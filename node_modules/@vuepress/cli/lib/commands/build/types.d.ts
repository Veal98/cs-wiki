/**
 * Type of `dev` command function
 */
export declare type BuildCommand = (sourceDir?: string, commandOptions?: BuildCommandOptions) => Promise<void>;
/**
 * CLI options of `build` command
 */
export interface BuildCommandOptions {
    dest?: string;
    temp?: string;
    cache?: string;
    debug?: boolean;
    config?: string;
    cleanTemp?: boolean;
    cleanCache?: boolean;
}
