import type { App, Page } from '@vuepress/core';
/**
 * Event handler for page add event
 *
 * Returns the added page
 */
export declare const handlePageAdd: (app: App, filePath: string) => Promise<Page | null>;
