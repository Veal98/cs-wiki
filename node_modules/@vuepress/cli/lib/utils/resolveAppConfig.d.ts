import type { AppConfig } from '@vuepress/core';
/**
 * Resolve app config according to:
 *
 * - default options
 * - cli options
 * - user config file
 */
export declare const resolveAppConfig: ({ defaultAppConfig, cliAppConfig, userConfig, }: {
    defaultAppConfig: Partial<AppConfig>;
    cliAppConfig: Partial<AppConfig>;
    userConfig: Partial<AppConfig>;
}) => AppConfig | null;
