import type { AppConfig } from '@vuepress/core';
/**
 * Resolve app config according to command options of cli
 */
export declare const resolveCliAppConfig: (sourceDir: string, commandOptions: Partial<AppConfig>, cwd?: string) => Partial<AppConfig> & Pick<AppConfig, 'source'>;
